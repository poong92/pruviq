# Moved to backend/deploy/cloudflared/

The systemd unit and runbook now live alongside the existing cloudflared
config in `backend/deploy/cloudflared/`. See that directory's README.md
for activation instructions.

This file and `infrastructure/cloudflared.service` are removed.
