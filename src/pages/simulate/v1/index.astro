---
import Layout from "../../../layouts/Layout.astro";
import SimulatorV1 from "../../../components/simulator/v1/SimulatorV1";
import { useTranslations } from "../../../i18n/index";

const t = useTranslations("en");
const title = "PRUVIQ Simulator — Click a preset, see proof";
const description =
  "Quick-Start crypto strategy simulator. Click a preset, see a real backtest, compare it to our live OKX results. 7 curated presets, 240+ coins.";
---

<Layout title={title} description={description}>
  <meta slot="head" name="robots" content="noindex, nofollow" />
  <main class="min-h-screen bg-zinc-950">
    <SimulatorV1 client:only="preact" lang="en" />
  </main>
</Layout>
